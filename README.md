# installation

ToDo:
`pip install`

# configuration
setup your settings.py:

```
INSTALLED_APPS = [
    # your setup with allauth and allauth.socialaccount
    'allauth.account',
    'allauth.socialaccount',
    'janus_provider',
    
]
```